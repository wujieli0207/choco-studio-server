import { TransoformInterceptor } from './transoform.interceptor';

describe('TransoformInterceptor', () => {
  it('should be defined', () => {
    expect(new TransoformInterceptor()).toBeDefined();
  });
});
